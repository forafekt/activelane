# ActiveLane Workbench frontend

This Vue application is the Wails webview entry point. It composes the shared
`@activelane/workbench` shell with Wails-native capabilities from
`src/services/native.ts`. Run it through `wails3 dev` from the parent directory
so bindings and the native runtime are available.
