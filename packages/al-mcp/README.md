# @activelane/mcp

Local-first MCP primitives for ActiveLane. The package provides:

- typed JSON-RPC-style request and response envelopes
- in-memory transport for extension-to-extension communication
- MCP client and server implementations
- endpoint and capability registries for local discovery
- a router for future remote MCP server adapters
